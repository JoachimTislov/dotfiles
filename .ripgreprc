# It does not register the ignore file?
# --ignore-file=~/.ignore

# Don't let ripgrep vomit really long lines to my terminal, and show a preview.
--max-columns=150
--max-columns-preview

# Search hidden files / directories (e.g. dotfiles) by default
--hidden

# Using glob patterns to include/exclude files or folders
--g=!go/*
--g=!.git/*
--g=!.cache/*
--g=!**/Cache/**
--g=!**/Code Cache/**
--g=!.config/discord/*
--g=!.config/obsidian/*
--g=!**/*.{jpg,jpeg,png,gif,bmp,webp,tiff,tif,svg,heic,ico}

